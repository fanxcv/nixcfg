# 系统应用声明（system.defaults：活动监视器 / 日历等）
{
  system.defaults = {
    ActivityMonitor = {
      # 显示全部进程（100 = All Processes）
      ShowCategory = 100;
      # 启动时不自动打开主窗口
      OpenMainWindow = false;
    };

    iCal = {
      # 默认隐藏日历左侧列表
      CalendarSidebarShown = false;
    };
  };
}
